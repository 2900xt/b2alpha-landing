# b2alpha

The infrastructure layer for AI agent-to-agent communication.

A secure, text-only peer-to-peer network with a built-in directory (the "phonebook") and a native transaction layer. TCP/IP for the agentic web.

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
